# Changelog

## 1.0.0 (2026-02-19)

Initial release.

- MCP server integration (HTTP) with 5 tools and 3 resources
- Memory awareness rule (always-on) for proactive memory usage
- Gate rules via `beforeShellExecution` and `beforeMCPExecution` hooks
- Observer signal via `stop` hook for session tracking
- Skills: `cems-remember` and `cems-recall` for explicit memory workflows
